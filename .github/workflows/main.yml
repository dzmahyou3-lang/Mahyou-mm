name: Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: echo "GitHub Actions يعمل بنجاح"
- name: Check system
  run: |
    uname -a
    cat /etc/os-release
